# 数据字典查询台内网部署手册

## 1. 部署目标

本系统是纯前端静态应用，适合部署到行内 Web 服务器。生产环境只需发布前端壳子，不携带 `dp_ial.xlsx`、`rcvp.xlsx` 等业务数据文件；使用人员从内网目录选择 Excel 导入后，即可在浏览器中查询。

系统不需要应用服务、数据库、外网访问或账号密码。Excel 解析、搜索和本地索引均在用户浏览器内完成。

```text
内网 Web 服务器（仅静态壳子）
            │ HTTPS / HTTP
            ▼
用户 Chrome / Edge
  ├─ 选择并导入 DP_IAL.xlsx
  ├─ 选择并导入 RCVP.xlsx
  └─ IndexedDB：本浏览器、本用户、本系统地址的本地索引
```

> 重要：IndexedDB 不会在用户之间共享。每台终端、每个浏览器配置文件首次使用时，都需要自行导入所需的数据源。这是“数据不随壳包外带”的安全边界，同时也是当前版本的使用方式。

## 2. 交付物与前置条件

### 2.1 交付物

向内网移交的生产包仅包含构建后的 `dist` 目录内容，例如：

```text
data-dictionary-query-desk/
├─ index.html
└─ assets/
   ├─ index-*.js
   └─ index-*.css
```

不要将下列文件放入交付包或 Web 根目录：

- `dp_ial.xlsx`
- `rcvp.xlsx`
- 源码目录 `src`
- `node_modules`
- 测试、覆盖率报告、PRD 等开发资料

业务 Excel 由内网数据管理员在需要时提供给使用人员，或保存在受控的内网共享目录。

> **可选组件——MySQL 桥服务**：不随壳包交付（部署方式见 5.4 节）。只有启用"连接数据库"直连导入功能时才需要；默认部署（Excel / DDL 导入、查询、AI 洞察思路）完全不需要它。

### 2.2 浏览器要求

- 推荐：Microsoft Edge 或 Google Chrome 的当前受支持版本。
- 浏览器必须允许本站点使用 IndexedDB；无痕/InPrivate 模式、浏览器“退出即清理站点数据”策略可能导致每次重新导入。
- 建议将系统部署为内网 HTTPS；HTTP 也可运行，但 HTTPS 更利于统一浏览器安全策略。

### 2.3 Web 服务器要求

任一可提供静态文件的内网 Web 服务器即可，例如 Nginx、IIS、Apache 或内部静态站点平台。

建议将应用部署在站点根路径，例如 `https://data-dictionary.intra/`。当前构建使用根路径资源；如必须部署在子路径（如 `/dictionary/`），需在构建前设置 Vite 的 `base` 并重新构建。

## 3. 制作“无数据壳包”

以下操作在开发/构建机完成，不在内网服务器上执行。

1. 安装依赖并执行质量检查。

   ```powershell
   npm ci
   npm run typecheck
   npm run lint
   npm run test:coverage
   ```

2. 正常构建，然后仅从构建产物中移除默认数据文件，确保壳包不携带业务数据。

   ```powershell
   npm run build
   Remove-Item -LiteralPath .\dist\dp_ial.xlsx
   ```

   此操作仅删除可重新生成的 `dist` 产物，不修改源码目录中的原始 Excel。

3. 检查 `dist` 目录，确认没有 `.xlsx` 文件。

   ```powershell
   Get-ChildItem -Path .\dist -Recurse -Filter *.xlsx
   ```

   该命令应无输出。

4. 将 `dist` 目录内容打包为 ZIP，作为内网交付物。发布前可执行：

   ```powershell
   Compress-Archive -Path .\dist\* -DestinationPath .\数据字典查询台-壳包.zip -Force
   ```

## 4. 发布到内网 Web 服务器

### 4.1 Nginx 示例

将壳包解压到，例如：`D:\www\data-dictionary\`。

```nginx
server {
    listen 443 ssl;
    server_name data-dictionary.intra;

    root D:/www/data-dictionary;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源采用构建后的内容指纹，可长期缓存
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

部署后访问 `https://data-dictionary.intra/`。首次因未携带默认 Excel，会出现“默认数据源未加载”页面；这是预期表现，页面会提供“导入数据字典”按钮。

### 4.2 IIS 要点

1. 创建网站或应用程序，物理路径指向壳包解压目录。
2. 启用“静态内容”。
3. 默认文档包含 `index.html`。
4. 建议使用 HTTPS 绑定。
5. 确保 `.js`、`.css`、`.svg`、字体等静态文件未被安全策略拦截。

本应用没有服务端接口，不需要配置反向代理、数据库连接串或服务账号。

## 5. 首次启用与数据导入

### 5.1 导入数仓数据源

1. 打开内网系统地址。
2. 在“默认数据源未加载”页面点击“导入数据字典”。
3. 选择内网提供的 `dp_ial.xlsx`。
4. 等待本地解析完成；完成后自动进入 DP_IAL 数仓工作区。
5. 在浏览器页面内搜索表、字段、标准或公共代码，确认可查询。

### 5.2 导入零售集市数据源

1. 在右上角点击“导入文件”。
2. 选择 `rcvp.xlsx`。
3. 完成后应用自动进入 RCVP 零售集市工作区。
4. 左侧“数据工作区”可切换 DP_IAL 与 RCVP。
5. 在 RCVP 字段详情中查看“数仓血缘与加工”，点击“跳转至 DP_IAL 字段”可定位到数仓来源字段。

两份数据分别保存在 IndexedDB 中；导入 RCVP 不会覆盖 DP_IAL。

### 5.3 从 MySQL 数据库直连导入（可选，需先部署桥服务）

1. 在右上角点击"连接数据库"。
2. 填写主机 / 端口 / 数据库 / 用户 / 密码，点击蓝色"测试连接"，应显示"已连接（MySQL 版本号）"。
3. 点击"获取表清单"，勾选要导入的表（显示表名与表注释，支持全选/清空）。
4. 点击"导入选中表"：系统取回各表的 `SHOW CREATE TABLE` 结果并在浏览器本地解析入库。
5. 导入结果进入数仓工作区，数据源名显示为 `MySQL 库名@主机`；会覆盖该工作区原有数据源，与"粘贴 DDL"导入行为一致。

### 5.4 部署 MySQL 桥服务（管理员，可选）

浏览器无法直连 MySQL（TCP 协议受限），"连接数据库"功能依赖一个仅内网使用的桥服务做翻译。它是纯 Node 小服务（`server/mysql-bridge.mjs`），只开放三个固定接口（测试连接 / 表清单 / 取 DDL），**不透传任意 SQL**；查询值全部参数绑定，表名/库名等无法绑定的标识符走白名单校验并与服务端实查清单比对。

**部署前提**：一台内网机器（可与 Nginx 同机），Node.js 20+，能访问目标 MySQL。

**安装与启动**：

```bash
# 拷贝本仓库（或至少 server/ 目录与 package.json、package-lock.json）
npm ci
npm run bridge          # 默认监听 http://127.0.0.1:4310
# 自定义端口：PORT=4321 npm run bridge（Windows PowerShell：$env:PORT=4321; npm run bridge）
```

**常驻运行**：

- Linux：systemd 单元示例

  ```ini
  [Unit]
  Description=Data Dictionary MySQL Bridge
  After=network.target

  [Service]
  WorkingDirectory=/opt/data-dictionary
  ExecStart=/usr/bin/node server/mysql-bridge.mjs
  Environment=PORT=4310
  Restart=always
  User=ddq

  [Install]
  WantedBy=multi-user.target
  ```

- Windows：建议用 NSSM 注册为服务（`nssm install ddq-bridge "C:\node.exe" "C:\data-dictionary\server\mysql-bridge.mjs"`），或任务计划程序开机启动。

**HTTPS 站点必须反代**：查询台以 HTTPS 访问时，浏览器会拦截对 `http://` 桥地址的请求（混合内容）。在 Nginx 站点中增加：

```nginx
location /api/mysql/ {
    proxy_pass http://127.0.0.1:4310;
    proxy_set_header Host $host;
}
```

然后在对话框的"桥服务设置"里把桥地址填为 `https://data-dictionary.intra`（同域，走反代）。

**安全基线**：

- 桥服务仅监听内网/本机，防火墙限制来源，绝不暴露公网；
- 连接 MySQL 建议使用**只读账号**（仅需 SELECT 与 SHOW 权限）；
- 桥不落盘保存任何密码：凭据只在单次请求内存中使用；
- 桥开启多语句与 localInfile 均已禁用，无任意 SQL 执行面。

## 6. 验收清单

部署人员可按以下清单验收：

- [ ] 发布目录不含任何 `.xlsx` 数据文件。
- [ ] 首页可打开，并显示“导入数据字典”入口。
- [ ] 导入 DP_IAL 后可查询中文表名、表结构和字段定义。
- [ ] 导入 RCVP 后可切换双工作区。
- [ ] RCVP 字段血缘可跳转到对应 DP_IAL 字段。
- [ ] 刷新页面后，已导入的工作区能够从 IndexedDB 恢复。
- [ ] 清理浏览器站点数据后，系统回到导入状态，且不会保留业务数据。
- [ ] （可选，启用桥服务时）"连接数据库"可测试连通并导入所选 MySQL 表结构。

## 7. 日常运维

### 数据更新

数据管理员提供新版 Excel 后，用户在系统中点击“导入文件”并选择新版文件即可更新对应工作区。本地索引会被新版导入结果覆盖。

若需要让大量终端同时更新，建议在内网共享目录中发布带版本号的 Excel，由各用户按统一通知导入。当前版本不提供服务端集中推送或共享索引。

### 浏览器缓存与故障处理

| 现象 | 处理方式 |
| --- | --- |
| 刷新后数据消失 | 检查是否使用无痕模式、是否被浏览器策略清理站点数据；必要时重新导入 Excel。 |
| 页面提示默认数据源未加载 | 对无数据壳包是正常状态，直接点击“导入数据字典”。 |
| 导入失败 | 检查文件是否为 `.xlsx/.xls`，确认未损坏、未被 Excel 独占锁定，并使用支持的 DP_IAL 或 RCVP 排版。 |
| 同事看不到我导入的数据 | 正常现象：索引仅保存在本机浏览器。对方需自行导入。 |
| 需要清空本机数据 | 在浏览器站点设置中清除此系统地址的站点数据，然后重新导入。 |
| "连接数据库"测试失败 | 按提示排查：地址/端口/防火墙（无法连接）、账号密码（鉴权失败）、桥服务未启动（配置区内"桥服务设置"检查地址，服务器上 `npm run bridge`）。 |
| HTTPS 站点连不上桥 | 浏览器拦截了 http 桥地址（混合内容）：按 5.4 配置 Nginx 反代并把桥地址改为同域 HTTPS。 |

### 版本升级

1. 在构建机生成新的无数据壳包。
2. 备份内网 Web 根目录中的旧壳包。
3. 覆盖发布新的 `index.html` 和 `assets`。
4. 通知用户强制刷新页面（`Ctrl + F5`）。
5. 一般不需要重新导入 Excel；若数据字典本身有更新，按“数据更新”流程重新导入。

## 8. 安全与边界说明

- 不上传 Excel：文件解析在用户浏览器本地执行。
- 默认无后端：系统不保存业务数据到服务器。
- 可选 MySQL 桥服务为**只读**拉取 DDL 的翻译层：不写库、不落盘凭据、不透传任意 SQL；启用与否由部署方决定（见 5.4）。
- AI 洞察思路只访问使用者自行配置的行内模型网关，配置与结果仅保存在本机浏览器；生成时会把数据源的表名、主题分布与字段引用统计发送至该网关。
- 不访问外网：部署与运行不依赖外部接口、CDN 或第三方登录。
- 本地索引是浏览器站点数据，不等同于加密保险箱；终端权限、浏览器策略和内网访问控制仍应遵循行内安全要求。
- 当前前端暂不展示、搜索修订记录；数据表、字段、标准、公共代码与 RCVP 血缘功能不受影响。
