# 数据字典查询台内网部署手册

## 1. 部署目标

本系统是纯前端静态应用，适合部署到行内 Web 服务器。生产环境只需发布前端壳子，不携带 `dp_ial.xlsx`、`rcvp.xlsx` 等业务数据文件；使用人员从内网目录选择 Excel 导入后，即可在浏览器中查询。

系统不需要应用服务、数据库、外网访问或账号密码。Excel 解析、搜索和本地索引均在用户浏览器内完成。

```text
内网 Web 服务器（仅静态壳子）
            │ HTTPS / HTTP
            ▼
用户 Chrome / Edge
  ├─ 选择并导入 DP_IAL.xlsx
  ├─ 选择并导入 RCVP.xlsx
  └─ IndexedDB：本浏览器、本用户、本系统地址的本地索引
```

> 重要：IndexedDB 不会在用户之间共享。每台终端、每个浏览器配置文件首次使用时，都需要自行导入所需的数据源。这是“数据不随壳包外带”的安全边界，同时也是当前版本的使用方式。

## 2. 交付物与前置条件

### 2.1 交付物

向内网移交的生产包仅包含构建后的 `dist` 目录内容，例如：

```text
data-dictionary-query-desk/
├─ index.html
└─ assets/
   ├─ index-*.js
   └─ index-*.css
```

不要将下列文件放入交付包或 Web 根目录：

- `dp_ial.xlsx`
- `rcvp.xlsx`
- 源码目录 `src`
- `node_modules`
- 测试、覆盖率报告、PRD 等开发资料

业务 Excel 由内网数据管理员在需要时提供给使用人员，或保存在受控的内网共享目录。

### 2.2 浏览器要求

- 推荐：Microsoft Edge 或 Google Chrome 的当前受支持版本。
- 浏览器必须允许本站点使用 IndexedDB；无痕/InPrivate 模式、浏览器“退出即清理站点数据”策略可能导致每次重新导入。
- 建议将系统部署为内网 HTTPS；HTTP 也可运行，但 HTTPS 更利于统一浏览器安全策略。

### 2.3 Web 服务器要求

任一可提供静态文件的内网 Web 服务器即可，例如 Nginx、IIS、Apache 或内部静态站点平台。

建议将应用部署在站点根路径，例如 `https://data-dictionary.intra/`。当前构建使用根路径资源；如必须部署在子路径（如 `/dictionary/`），需在构建前设置 Vite 的 `base` 并重新构建。

## 3. 制作“无数据壳包”

以下操作在开发/构建机完成，不在内网服务器上执行。

1. 安装依赖并执行质量检查。

   ```powershell
   npm ci
   npm run typecheck
   npm run lint
   npm run test:coverage
   ```

2. 正常构建，然后仅从构建产物中移除默认数据文件，确保壳包不携带业务数据。

   ```powershell
   npm run build
   Remove-Item -LiteralPath .\dist\dp_ial.xlsx
   ```

   此操作仅删除可重新生成的 `dist` 产物，不修改源码目录中的原始 Excel。

3. 检查 `dist` 目录，确认没有 `.xlsx` 文件。

   ```powershell
   Get-ChildItem -Path .\dist -Recurse -Filter *.xlsx
   ```

   该命令应无输出。

4. 将 `dist` 目录内容打包为 ZIP，作为内网交付物。发布前可执行：

   ```powershell
   Compress-Archive -Path .\dist\* -DestinationPath .\数据字典查询台-壳包.zip -Force
   ```

## 4. 发布到内网 Web 服务器

### 4.1 Nginx 示例

将壳包解压到，例如：`D:\www\data-dictionary\`。

```nginx
server {
    listen 443 ssl;
    server_name data-dictionary.intra;

    root D:/www/data-dictionary;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源采用构建后的内容指纹，可长期缓存
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

部署后访问 `https://data-dictionary.intra/`。首次因未携带默认 Excel，会出现“默认数据源未加载”页面；这是预期表现，页面会提供“导入数据字典”按钮。

### 4.2 IIS 要点

1. 创建网站或应用程序，物理路径指向壳包解压目录。
2. 启用“静态内容”。
3. 默认文档包含 `index.html`。
4. 建议使用 HTTPS 绑定。
5. 确保 `.js`、`.css`、`.svg`、字体等静态文件未被安全策略拦截。

本应用没有服务端接口，不需要配置反向代理、数据库连接串或服务账号。

## 5. 首次启用与数据导入

### 5.1 导入数仓数据源

1. 打开内网系统地址。
2. 在“默认数据源未加载”页面点击“导入数据字典”。
3. 选择内网提供的 `dp_ial.xlsx`。
4. 等待本地解析完成；完成后自动进入 DP_IAL 数仓工作区。
5. 在浏览器页面内搜索表、字段、标准或公共代码，确认可查询。

### 5.2 导入零售集市数据源

1. 在右上角点击“导入文件”。
2. 选择 `rcvp.xlsx`。
3. 完成后应用自动进入 RCVP 零售集市工作区。
4. 左侧“数据工作区”可切换 DP_IAL 与 RCVP。
5. 在 RCVP 字段详情中查看“数仓血缘与加工”，点击“跳转至 DP_IAL 字段”可定位到数仓来源字段。

两份数据分别保存在 IndexedDB 中；导入 RCVP 不会覆盖 DP_IAL。

## 6. 验收清单

部署人员可按以下清单验收：

- [ ] 发布目录不含任何 `.xlsx` 数据文件。
- [ ] 首页可打开，并显示“导入数据字典”入口。
- [ ] 导入 DP_IAL 后可查询中文表名、表结构和字段定义。
- [ ] 导入 RCVP 后可切换双工作区。
- [ ] RCVP 字段血缘可跳转到对应 DP_IAL 字段。
- [ ] 刷新页面后，已导入的工作区能够从 IndexedDB 恢复。
- [ ] 清理浏览器站点数据后，系统回到导入状态，且不会保留业务数据。

## 7. 日常运维

### 数据更新

数据管理员提供新版 Excel 后，用户在系统中点击“导入文件”并选择新版文件即可更新对应工作区。本地索引会被新版导入结果覆盖。

若需要让大量终端同时更新，建议在内网共享目录中发布带版本号的 Excel，由各用户按统一通知导入。当前版本不提供服务端集中推送或共享索引。

### 浏览器缓存与故障处理

| 现象 | 处理方式 |
| --- | --- |
| 刷新后数据消失 | 检查是否使用无痕模式、是否被浏览器策略清理站点数据；必要时重新导入 Excel。 |
| 页面提示默认数据源未加载 | 对无数据壳包是正常状态，直接点击“导入数据字典”。 |
| 导入失败 | 检查文件是否为 `.xlsx/.xls`，确认未损坏、未被 Excel 独占锁定，并使用支持的 DP_IAL 或 RCVP 排版。 |
| 同事看不到我导入的数据 | 正常现象：索引仅保存在本机浏览器。对方需自行导入。 |
| 需要清空本机数据 | 在浏览器站点设置中清除此系统地址的站点数据，然后重新导入。 |

### 版本升级

1. 在构建机生成新的无数据壳包。
2. 备份内网 Web 根目录中的旧壳包。
3. 覆盖发布新的 `index.html` 和 `assets`。
4. 通知用户强制刷新页面（`Ctrl + F5`）。
5. 一般不需要重新导入 Excel；若数据字典本身有更新，按“数据更新”流程重新导入。

## 8. 安全与边界说明

- 不上传 Excel：文件解析在用户浏览器本地执行。
- 不写服务端数据库：系统没有后端 API，也不保存业务数据到服务器。
- 不访问外网：部署与运行不依赖外部接口、CDN 或第三方登录。
- 本地索引是浏览器站点数据，不等同于加密保险箱；终端权限、浏览器策略和内网访问控制仍应遵循行内安全要求。
- 当前前端暂不展示、搜索修订记录；数据表、字段、标准、公共代码与 RCVP 血缘功能不受影响。
