# 数据字典查询台 PRD（评审稿 v0.1）

## 1. 文档目的

把目前依赖 Excel 逐表、逐字段查找的工作，改造成一个本地优先的桌面数据字典查询工具。用户导入 Excel 后，可以通过中文名、英文名、字段名、标准编号、公共代码和业务主题快速定位表与字段，并在一个页面内查看完整结构及其关联信息。

本稿只定义产品与首版范围，暂不进入代码实现。

## 2. 背景与问题

当前工作方式存在四个主要问题：

1. 查找路径长：需要先在“目录”找表，再跳转“数据字典”找字段，遇到标准或代码还要继续切 sheet。
2. 记忆成本高：同一对象同时存在中文名称、英文表名、英文字段名、标准编号等多个入口，Excel 的跳转能力不足以支持模糊检索。
3. 关联关系隐蔽：字段与数据标准、公共代码、历史修订的关联分散在不同 sheet 中，难以判断一个字段的完整语义。
4. 版本不透明：导入的新文件可能覆盖旧文件，无法快速确认当前查询使用的是哪个版本、哪些内容发生过变化。

目标不是把 Excel 搬到网页上，而是将 Excel 作为数据源，整理成可检索、可关联、可追溯的本地数据字典。

## 3. 数据依据

首版以 `dp_ial.xlsx` 为样本数据源，已确认：

| 数据对象 | 来源 sheet | 规模/字段 |
|---|---|---|
| 表目录 | 目录 | 126 张表；包含表中文名、英文名、加载策略、频度、保留时效、业务范围、责任人、主题等 |
| 表字段 | 数据字典 | 10,902 个字段；包含字段序号、中文名、英文名、两套数据库类型、主键/分布键/分区键、标准编号、公共代码、备注等 |
| 数据标准 | 数据标准V2.0 | 标准编号、信息项中英文名称、主题、数据类型、长度、精度 |
| 公共代码 | 公共代码 | 769 类代码、22,139 条代码明细；包含代码值、代码值描述、引用标准编号/名称 |
| 变更记录 | 历史修订记录 | 表名、修改前后字段信息、修改原因、操作类型、操作人、修改时间 |
| 修订附件 | 附件1、附件2 | 字段映射标准删除和数据类型修改记录 |

导入器需要支持上述固定结构，同时保留未知 sheet，避免源文件扩展时丢失数据。

## 4. 产品定位

**产品名（工作名）：数据字典查询台**

一个面向数据开发、数据治理、测试和需求分析人员的本地桌面数据字典客户端。核心价值是：

> 输入一个中文业务词、英文标识或标准编号，在一次搜索内找到“它在哪张表、哪个字段、是什么含义、什么类型、关联哪些代码和标准、最近是否改过”。

## 5. 用户与核心场景

### 5.1 目标用户

- 数据开发：确认表名、字段名、类型、主键和分布键。
- 数据治理/标准管理：查看字段与标准编号、公共代码及修订记录的关系。
- 测试与联调：根据中文业务含义反查字段，核对类型和代码值。
- 需求分析：从业务词找到相关表，并比较同名字段在不同表中的定义。

### 5.2 高频场景

1. “法人代码”对应哪些表和字段？每张表的类型、主键属性是否一致？
2. “客户编号”英文是什么？在哪些表出现？
3. `a_pub_dpsit_acct_bal_view` 有哪些字段？它的分区键、标准编号和公共代码是什么？
4. 标准编号 `SCBTS0000046` 对应哪些字段？
5. 某字段最近是否修改过？修改原因和前后定义是什么？
6. 某个公共代码有哪些值和描述？被哪些字段引用？

## 6. 产品目标与非目标

### 6.1 首版目标

- Excel 导入后自动识别标准 sheet、校验表头并建立本地索引。
- 支持表、字段、数据标准、公共代码、修订记录的统一搜索。
- 支持从搜索结果进入表详情、字段详情，并一键查看关联对象。
- 支持导入版本管理，保证查询结果可追溯。
- 支持复制表名、字段名、类型、标准编号和可直接使用的 SQL 字段片段。

### 6.2 首版不做

- 不查询生产数据库，不展示真实业务数据。
- 不修改原始 Excel，不承担数据标准审批或发布流程。
- 不做多人在线协作、权限体系和云端同步。
- 不做 SQL 执行器、血缘分析和自动生成完整建表脚本。
- 不把所有修订附件强行合并为一个“当前事实”；附件内容作为可追溯的修订来源展示。

## 7. 核心产品流程

```text
导入 Excel
  -> 识别 sheet / 表头 / 行数
  -> 数据质量检查
  -> 生成本地索引与关联关系
  -> 查询表/字段/标准/代码
  -> 查看详情
  -> 横向跳转关联对象
  -> 复制或导出当前结果
```

## 8. 功能需求

### 8.1 首页 / 全局搜索

首页以搜索为主，不做营销式首页。

- 一个支持中文、英文、标准编号、代码值的全局搜索框。
- 默认搜索范围为全部对象；可切换“表 / 字段 / 数据标准 / 公共代码 / 修订记录”。
- 支持精确匹配、包含匹配、前缀匹配；英文不区分大小写。
- 搜索结果按对象类型分组，并显示匹配字段、中文含义、英文标识、所属表和来源版本。
- 支持最近搜索和最近查看，保存在本机。
- 搜索无结果时，给出“可能存在于未识别 sheet / 当前版本不存在 / 检查拼写”的明确提示。

### 8.2 表目录

- 左侧按主题或表中文名浏览，支持主题筛选。
- 表项展示：表中文名、表英文名、字段数量、主题、加载频度。
- 支持按中文名、英文名、责任人、业务范围筛选。
- 支持按字段数量排序，便于快速定位大表。

### 8.3 表详情

表详情是首版的主工作区，建议采用“上方表概览 + 下方字段表格”的布局。

表概览显示：

- 表中文名与英文名。
- 业务范围、归集信息、主题、责任人。
- 加载策略、加载频度、保留时效、使用方式。
- 当前数据版本、来源文件和导入时间。

字段表格显示：

- 序号。
- 字段中文名。
- 字段英文名。
- 高斯类型 / SR 类型。
- 主键、分布键、分区键标识。
- 数据标准长度。
- 映射标准编号。
- 公共代码。
- 备注。

字段表格要求：固定表头、列宽可调整、长表虚拟滚动、字段中文/英文快速筛选、复制单元格和复制整行。

### 8.4 字段详情

点击字段后打开右侧详情面板或独立详情页，避免离开当前表上下文。

- 基本定义：所属表、字段序号、中文名、英文名。
- 存储定义：高斯类型、SR 类型、数据标准长度。
- 键属性：主键、分布键、分区键。
- 标准关系：映射标准编号、数据标准中文/英文名称、主题、标准类型/长度/精度。
- 代码关系：公共代码名称、引用标准编号；可继续进入代码明细。
- 备注与来源：原始 sheet、来源行号、导入版本。
- 修订记录：修改时间、前后定义、原因、操作人。
- 操作：复制字段名、复制类型、复制 SQL 字段片段、打开所属表。

### 8.5 数据标准与公共代码

- 数据标准列表：按标准编号、中文名、英文简称、主题、数据类型筛选。
- 数据标准详情：展示标准定义及被哪些字段引用。
- 公共代码列表：按代码名称、代码英文名、代码值、描述、标准编号查询。
- 公共代码详情：展示所有代码值/描述，并反查引用该代码的字段和表。
- 代码值支持复制；支持按代码值描述模糊检索。

### 8.6 修订与版本

- 每次导入生成一个本地版本，默认以文件名 + 导入时间标识。
- 默认查询“当前版本”；用户可切换历史版本。
- 显示版本摘要：表数量、字段数量、标准数量、代码明细数量、异常数量。
- 首版支持按表名、字段名、修改时间、操作类型查看修订记录。
- 二期再增加版本间差异对比；首版至少保留原始记录和来源定位。

### 8.7 Excel 导入与质量检查

导入向导分 4 步：

1. 选择文件：支持 `.xlsx`，文件始终在本地处理。
2. 识别结构：展示识别到的 sheet、表头、数据行数；固定 sheet 自动匹配，其他 sheet 标记为“未映射”。
3. 数据检查：检查必需列、空值、重复表名/字段名、同一表内序号重复、标准编号关联缺失、公共代码引用缺失。
4. 确认导入：展示预计新增/更新/异常统计，用户确认后建立索引。

导入失败时必须给出可定位到 sheet、行号、列名的错误，不允许只显示“导入失败”。

### 8.8 导出与复制

- 复制字段名、表名、字段定义。
- 复制当前筛选结果为 Markdown 表格或 CSV。
- 导出当前表结构为 Excel/CSV 属于二期；首版只要求复制和轻量文本导出。

## 9. 信息架构

```text
数据字典查询台
├─ 全局搜索
├─ 表目录
│  └─ 表详情
│     └─ 字段详情
├─ 数据标准
│  └─ 标准详情
├─ 公共代码
│  └─ 代码详情
├─ 修订记录
└─ 数据源与导入
   ├─ 当前版本
   ├─ 历史版本
   └─ 导入检查报告
```

## 10. 推荐 C/S 架构

### 10.1 客户端（C）

- 桌面 UI：负责搜索、列表、详情、导入向导、复制和导出。
- 本地状态：当前数据版本、筛选条件、最近查询、界面偏好。
- 不直接在 UI 中解析大型 Excel，避免页面卡顿。

### 10.2 本地服务（S）

- Excel 解析器：读取 workbook、识别 sheet 和表头。
- 规范化层：统一空值、大小写、日期、类型字符串和标识符。
- 索引层：为中文名、英文名、标准编号、代码值、备注等建立全文/前缀索引。
- 关联层：维护表-字段-标准-公共代码-修订记录之间的关系。
- 查询 API：分页、筛选、排序、详情和复制片段所需的数据服务。

### 10.3 本地存储建议

首版推荐 SQLite + FTS5：

- 适合单机离线使用和 1-10 万级元数据规模。
- 便于事务化导入、版本回滚和全文检索。
- 原始 Excel 不改动；数据库保存解析后的规范化数据和来源定位。
- 每条记录至少保留 `source_file`、`source_sheet`、`source_row`、`import_version`。

### 10.4 技术实现建议（待评审）

- 桌面容器：Tauri。
- 前端：Vue 3 + TypeScript。
- 本地服务/命令：Rust 或 Node.js，按团队熟悉度决定。
- UI 组件：优先使用已有组件库，表格采用虚拟滚动。
- 索引：SQLite FTS5，必要时增加拼音/分词字段。

## 11. 关键数据模型

```text
ImportVersion
  ├─ TableCatalog 1:N TableField
  ├─ DataStandard 1:N TableField
  ├─ PublicCodeSet 1:N PublicCodeItem
  ├─ TableField N:0..1 PublicCodeSet
  ├─ TableField 1:N RevisionRecord
  └─ UnknownSheet 1:N RawRow（仅用于追溯/后续扩展）
```

核心实体：

- `table_catalog`：表中文名、表英文名、主题、业务范围、加载策略等。
- `table_field`：表 ID、序号、字段中文名、字段英文名、类型、键属性、标准编号、公共代码等。
- `data_standard`：标准编号、中文名、英文简称、主题、数据类型、长度、精度。
- `public_code_set` / `public_code_item`：代码集、代码值、值描述、引用标准。
- `revision_record`：修改时间、表名、前后字段定义、原因、操作人。
- `import_version`：来源文件、导入时间、统计摘要、状态。

## 12. 视觉与交互方向

参考本地 `ian-xiaohei-illustrations`  的方法论，但不把数据字典做成插画页面。

### 12.1 继承的原则

- 小黑风格：白底、黑色结构线、留白、少量红/橙/蓝作为状态和重点标记；对应到产品中就是清晰的层级、可扫描的表格和克制的交互强调。

### 12.2 产品化落地

- 主背景以纯白或极浅中性灰为主，避免米色纸张纹理、玻璃拟态和科技蓝渐变。
- 主文字使用炭黑；红色表示异常/变更，橙色表示当前路径或选中关系，蓝色表示辅助信息/标准关系，绿色只用于通过状态。
- 面板和表格使用 4-8px 小圆角、细边框和明确分隔，不堆叠装饰卡片。
- 表格是主视觉，不在首屏放无关的统计图和大面积插画。
- 可在空状态、导入完成状态使用极简手绘线稿作为识别点，但不影响核心查询。
- 中英文并列时中文承担主要阅读，英文使用等宽字体或紧凑字重区分。

## 13. 首版验收标准

### 数据与导入

- 能成功导入当前 `dp_ial.xlsx`，并识别 7 个 sheet。
- 导入摘要至少能显示 126 张表、10,902 个字段、数据标准、公共代码和修订记录的数量。
- 能定位并提示必需列缺失、关键关系缺失和重复记录。
- 原始文件不被修改，查询结果能追溯到来源文件、sheet 和行号。

### 查询与详情

- 输入“法人代码”可返回字段结果，并显示所属表和字段英文名。
- 输入 `a_pub_dpsit_acct_bal_view` 可进入表详情并显示全部字段。
- 输入标准编号 `SCBTS0000046` 可看到标准信息及引用字段。
- 输入公共代码名称或代码值可进入代码详情，并反查引用字段。
- 字段详情可在不丢失当前上下文的情况下查看标准、代码和修订关联。

### 性能与体验

- 首次导入在本地完成，不要求网络或生产数据库权限。
- 已完成导入后，常用关键词搜索响应目标小于 300ms；长字段表滚动不明显卡顿。
- 用户能在 3 次点击内从搜索结果复制字段英文名和类型。
- 键盘可完成搜索、上下移动结果、打开详情和复制。

## 14. 迭代优先级

### P0：可用闭环

- Excel 导入与校验。
- 全局搜索。
- 表目录、表详情、字段详情。
- 标准/公共代码关联。
- 复制和来源追溯。

### P1：效率提升

- 多字段组合筛选。
- 历史版本切换。
- 版本差异对比。
- 收藏表/字段、最近访问。
- 拼音搜索、同义词和常用业务词别名。

### P2：治理扩展

- 血缘关系和引用分析。
- SQL/建表片段模板。
- 团队共享包或受控发布。
- 标准审批、变更流程与权限。

## 15. 待评审决策

1. 桌面容器是否接受 Tauri；若团队已有 Electron 经验，也可改为 Electron，但本地服务与索引边界保持不变。
2. 首版是否需要同时支持多个 Excel 数据源并进行跨源搜索，还是先以单个当前版本为主。
3. “公共代码”与“引用的公共代码”是否存在业务上的一对多/别名规则，需要在导入校验中明确。
4. 是否需要把“数据标准V2.0”作为当前唯一标准，还是保留标准版本字段以支持后续多版本。
5. 导出能力首版是否只做复制/CSV，还是需要直接生成建表 SQL。

## 16. 本版结论

首版应优先做成一个**本地可用、搜索优先、关联清晰、来源可追溯**的桌面查询台。最关键的产品变化不是把 Excel 换成漂亮界面，而是将“表、字段、标准、代码、修订”从分散 sheet 组织成连续的查询路径。
